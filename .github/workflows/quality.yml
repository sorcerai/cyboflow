name: Code Quality

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  quality-checks:
    name: Quality Checks
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup pnpm
      uses: pnpm/action-setup@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.15.1'
        cache: 'pnpm'

    - name: Install dependencies
      run: pnpm install

    - name: Run type checking
      run: pnpm typecheck

    - name: Run linting
      run: pnpm lint

  test:
    name: Tests
    runs-on: ubuntu-latest
    needs: quality-checks

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup pnpm
      uses: pnpm/action-setup@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.15.1'
        cache: 'pnpm'

    - name: Install dependencies
      run: pnpm install

    # The root `postinstall` (electron-builder install-app-deps) rebuilds
    # better-sqlite3 against the Electron ABI. The unit suite runs under host
    # Node (vitest), so restore the host-Node ABI or main DB tests throw
    # NODE_MODULE_VERSION mismatches. See CLAUDE.md. The rebuild is wrapped in
    # scripts/rebuild-better-sqlite3-host.mjs, which asserts a real :memory:
    # database opens post-rebuild and retries once with a forced source build
    # before failing loudly — a bare `pnpm rebuild` can silently leave a stale
    # wrong-arch prebuild in place.
    - name: Rebuild better-sqlite3 for host Node ABI
      run: node scripts/rebuild-better-sqlite3-host.mjs

    # The cyboflowMcpServer subprocess lifecycle tests spawn the BUILT bundle at
    # main/dist/.../cyboflowMcpServer.js — they cannot import the server, because
    # it runs main() and calls process.exit at module scope. `test:unit` does not
    # build, so without this step that artifact is absent in CI and those tests
    # skip themselves, silently removing the entire regression guard for the
    # orphaned-MCP-process fix while the suite still reports green. The test file
    # hard-fails (rather than skipping) when CI=true and the bundle is missing, so
    # deleting this step turns CI red instead of quietly losing the coverage.
    - name: Build main (MCP server bundle for subprocess tests)
      run: pnpm build:main

    # Headless unit chain (main + frontend vitest, schema parity, build scripts).
    # The Playwright E2E suite is intentionally excluded: the renderer cannot
    # bootstrap in CI without the Electron preload-injected electronTRPC bridge,
    # so it fails on every run regardless of the change. See CLAUDE.md; re-add an
    # E2E job once the Playwright config uses _electron.launch().
    - name: Run unit tests
      run: pnpm test:unit
      env:
        CI: true
      # The chain is main suite + frontend suite + schema parity + release
      # scripts + ABI + build tests. On ubuntu runners the two vitest legs
      # alone approach 10 minutes; 10 was a timeout, not a pass.
      timeout-minutes: 25

  # Blocking mocked-SDK integration tier (Tier-3). Runs in PARALLEL with `test`
  # (both only need `quality-checks`), so the PR critical path stays at
  # max(test, integration), not their sum. Drives the real orchestrator stack
  # (WorkflowRegistry / RunLauncher / ApprovalRouter / chokepoint routers) plus
  # the real TypedEventNarrowing + MessageProjection read pipeline over a
  # migration-replay temp DB with only the SDK query() faked; the run lifecycle
  # loop itself is the headlessRun harness's (RunExecutor/ClaudeCodeManager
  # write-side coverage is a recorded follow-up — see the plan doc's
  # "Follow-ups"); no live `claude` CLI, no Electron.
  integration:
    name: Integration (mocked SDK)
    runs-on: ubuntu-latest
    needs: quality-checks
    timeout-minutes: 8

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup pnpm
      uses: pnpm/action-setup@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.15.1'
        cache: 'pnpm'

    - name: Install dependencies
      run: pnpm install

    # Same host-Node ABI flip the `test` job needs: the integration suite runs
    # under host Node (vitest) against real migration-replay temp DBs. See the
    # `test` job comment above and CLAUDE.md.
    - name: Rebuild better-sqlite3 for host Node ABI
      run: node scripts/rebuild-better-sqlite3-host.mjs

    - name: Run integration tests
      run: pnpm test:integration
      env:
        CI: true
      timeout-minutes: 8

  # Report-only coverage — no threshold yet (see docs/proposals/test-coverage-tightening-plan.md
  # Workstream C). Split into its own job (needs: test, if: always(),
  # continue-on-error) so its four ~always() steps stop inflating the PR critical
  # path — they gate nothing and roughly doubled the `test` job's wall-clock.
  # continue-on-error so a coverage-run hiccup never reds the workflow; this job
  # exists purely to make coverage visible while a baseline is established. The
  # text-reporter table (main/frontend vitest.config.ts both list 'text' first)
  # is captured to a log file and tailed into the job summary rather than parsing
  # a coverage-summary.json the 'text'/'json'/'html' reporter set doesn't produce.
  coverage:
    name: Coverage (report-only)
    runs-on: ubuntu-latest
    needs: test
    if: always()
    continue-on-error: true

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup pnpm
      uses: pnpm/action-setup@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.15.1'
        cache: 'pnpm'

    - name: Install dependencies
      run: pnpm install

    # Coverage runs main DB tests under host Node, so the same ABI flip applies.
    - name: Rebuild better-sqlite3 for host Node ABI
      run: node scripts/rebuild-better-sqlite3-host.mjs

    - name: Coverage (report-only, main)
      if: always()
      continue-on-error: true
      run: pnpm --filter main exec vitest run --coverage --reporter=dot | tee main-coverage.log
      env:
        CI: true
      timeout-minutes: 10

    - name: Coverage summary (main)
      if: always()
      continue-on-error: true
      run: |
        {
          echo "### Main coverage (report-only)"
          echo '```'
          head -n 60 main-coverage.log 2>/dev/null || echo "(no coverage output captured)"
          echo '```'
        } >> "$GITHUB_STEP_SUMMARY"

    - name: Coverage (report-only, frontend)
      if: always()
      continue-on-error: true
      run: pnpm --filter frontend exec vitest run --coverage --reporter=dot | tee frontend-coverage.log
      env:
        CI: true
      timeout-minutes: 10

    - name: Coverage summary (frontend)
      if: always()
      continue-on-error: true
      run: |
        {
          echo "### Frontend coverage (report-only)"
          echo '```'
          head -n 60 frontend-coverage.log 2>/dev/null || echo "(no coverage output captured)"
          echo '```'
        } >> "$GITHUB_STEP_SUMMARY"
