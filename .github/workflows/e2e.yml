name: E2E (smoke tier)

# Playwright smoke tier driving the built Electron bundle via
# _electron.launch() (tests/helpers/electronApp.ts), on a macOS runner
# (ship-target fidelity — a real display, no xvfb). The smoke tier is BLOCKING:
# promoted 2026-08-14 after 41/41 green nightlies since 2026-07-04 (step-level
# verified). It gates PRs to main and must stay in branch protection's required
# checks ("E2E smoke (macOS)"). The sdk-canaries and flake-watch jobs remain
# report-only and nightly-only (canaries need an authenticated `claude`;
# flake-watch is empty until a test is quarantined).

on:
  workflow_dispatch:
  pull_request:
    branches: [ main ]
  schedule:
    # 07:00 UTC nightly.
    - cron: '0 7 * * *'

jobs:
  e2e-smoke:
    name: E2E smoke (macOS)
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.15.1'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      # Build prereqs for the launched app. The root `postinstall`
      # (electron-builder install-app-deps) already rebuilds better-sqlite3 for
      # the Electron ABI, which is exactly what the LAUNCHED app needs — so no
      # host-Node rebuild here (this job runs no vitest). `build:main` +
      # `build:frontend` produce main/dist and frontend/dist.
      - name: Build main + frontend
        run: |
          pnpm run build:main
          pnpm run build:frontend

      - name: Install Playwright (no browser download needed for Electron)
        run: pnpm exec playwright install-deps || true

      # Blocking since 2026-08-14 (41/41 green nightlies). Drive the smoke tier
      # only — the 7-test minimal config with a proven CI track record.
      - name: Run smoke tier
        run: pnpm run test:ci:minimal
        env:
          CI: true
        timeout-minutes: 15

      - name: Upload Playwright artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-e2e-report
          path: |
            test-results/
            playwright-report/
          if-no-files-found: ignore
          retention-days: 7

  # Nightly SDK drift canaries (report-only). These hit the REAL Anthropic API
  # via the logged-in `claude` CLI, so they can only run where `claude` is on
  # PATH and authenticated — otherwise they skip cleanly with a notice. They
  # catch behavioral/protocol drift (WHEN canUseTool fires, event reordering,
  # new event kinds) that the mocked-SDK gate and `pnpm typecheck` cannot see.
  # Both continue-on-error — a canary must never red the nightly.
  sdk-canaries:
    name: SDK drift canaries (macOS)
    runs-on: macos-latest
    # Nightly/dispatch only — on hosted runners `claude` is never on PATH, so a
    # PR run would just spin up a macOS runner to skip both steps.
    if: github.event_name != 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.15.1'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      # Wire-shape canary: the real orchestrator day-gate against a live `claude`.
      # Proves the true pre-narrow wire bytes still satisfy TypedEventNarrowing.
      - name: Wire-shape canary (test:gate, real API)
        continue-on-error: true
        run: |
          if ! command -v claude >/dev/null 2>&1; then
            echo "::notice::claude not on PATH — skipping test:gate wire-shape canary"
            exit 0
          fi
          pnpm test:gate
        env:
          CI: true
        timeout-minutes: 20

      # Protocol canary: the standalone raw-SDK smoke probe. Decoupled from
      # main/src, so it validates the library surface directly.
      - name: Protocol canary (smoke:sdk, real API)
        continue-on-error: true
        run: |
          if ! command -v claude >/dev/null 2>&1; then
            echo "::notice::claude not on PATH — skipping smoke:sdk protocol canary"
            exit 0
          fi
          pnpm smoke:sdk
        env:
          CI: true
        timeout-minutes: 10

      # TODO(M7 follow-up): re-record-and-diff canary. Capture a fresh scrubbed
      # transcript from a real test:gate run and structurally diff it against a
      # committed reference to catch new event kinds / callback-ordering drift,
      # uploading a candidate artifact for a human to promote. Deferred: it needs
      # recorder/scrubber tooling (consistent-id map, tool_use↔tool_result pair
      # preservation) that intentionally does not exist yet.

  # Report-only flake-watch: runs any quarantined tests (*.quarantine.test.ts /
  # *.quarantine.itest.ts) that have been pulled out of the blocking configs.
  # Exits 0 when there are none, so this job is a no-op until a test is
  # quarantined. See docs/plans/ci-gate-mocked-sdk-integration.md "Flake
  # quarantine" for the promotion/demotion policy.
  flake-watch:
    name: Flake watch (quarantined tests, report-only)
    runs-on: macos-latest
    # Nightly/dispatch only — a no-op on PRs until something is quarantined.
    if: github.event_name != 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.15.1'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      # Quarantined main unit tests hit real better-sqlite3 like the blocking suite.
      - name: Rebuild better-sqlite3 for host Node ABI
        run: node scripts/rebuild-better-sqlite3-host.mjs

      # Each set runs under its OWNING config (alias/setupFiles/environment/pool) —
      # a bare `vitest run <files>` from the root would fall back to vitest's
      # default config (no @ alias, no electron mock, no jsdom) and error on
      # collection every night, making the two-green-nightlies promotion bar
      # unreachable. CYBOFLOW_RUN_QUARANTINE=1 lifts the quarantine exclude that
      # those configs otherwise apply.
      - name: Run quarantined tests (if any)
        continue-on-error: true
        run: |
          status=0
          main_unit=$(find main/src -type f -name '*.quarantine.test.ts' | sed 's|^main/||')
          fe_unit=$(find frontend/src -type f -name '*.quarantine.test.ts' | sed 's|^frontend/||')
          itests=$(find main/src -type f -name '*.quarantine.itest.ts')
          if [ -z "$main_unit$fe_unit$itests" ]; then
            echo "::notice::No quarantined tests — nothing to run."
            exit 0
          fi
          if [ -n "$main_unit" ]; then
            echo "Quarantined main unit tests:"; echo "$main_unit"
            (cd main && pnpm exec vitest run $main_unit) || status=1
          fi
          if [ -n "$fe_unit" ]; then
            echo "Quarantined frontend unit tests:"; echo "$fe_unit"
            (cd frontend && pnpm exec vitest run $fe_unit) || status=1
          fi
          if [ -n "$itests" ]; then
            echo "Quarantined integration tests:"; echo "$itests"
            pnpm exec vitest run --config vitest.config.integration.ts $itests || status=1
          fi
          exit $status
        env:
          CI: true
          CYBOFLOW_RUN_QUARANTINE: '1'
        timeout-minutes: 15
